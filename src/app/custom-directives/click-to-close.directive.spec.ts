import { ClickToCloseDirective } from './click-to-close.directive';

describe('ClickToCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickToCloseDirective();
    expect(directive).toBeTruthy();
  });
});
